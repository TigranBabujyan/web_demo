{
    "name": "web-demo-mcp",
    "version": "1.0.0",
    "description": "MCP server for Figma integration",
    "main": "figma-mcp-server.js",
    "scripts": {
        "start": "node figma-mcp-server.js",
        "kill": "powershell -Command \"Get-NetTCPConnection -LocalPort 3000,3001 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }; Write-Host 'Killed all dev servers on ports 3000 and 3001'\""
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^0.5.0",
        "axios": "^1.6.0"
    }
}